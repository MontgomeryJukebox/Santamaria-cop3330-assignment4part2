package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class TDManagerTest {

    @Test
    void getGroup() {
        // manually set group
        // assert getGroup() returns correctly
    }

    @Test
    void getGroups() {
        // manually create multiple groups, add them to temporary vector
        // add all groups() to tdmanager
        // for each group in vector:
        //      pop from tdmanager
        //      assert group exists and exists within vector
        //      remove from vector
        // ^ this should be alright as long as vector never grows from operations (which it shouldn't)
    }
}