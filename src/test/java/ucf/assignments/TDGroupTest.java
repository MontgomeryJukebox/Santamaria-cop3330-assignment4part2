package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class TDGroupTest {

    @Test
    void addList() {
        // create new list
        // add it to the group
        // check that list is now within the group's structure
    }

    @Test
    void removeList() {
        // addList() to group
        // check group size and elements
        // removeList() from group
        // check group size and elements, assert change followed through
    }
}